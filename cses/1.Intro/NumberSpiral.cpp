#include <bits/stdc++.h>
using namespace std;

using ll = long long;

int solve(int y, int x) {

    return 0;
}

int main() {
    printf("%f", (1/2 + sqrt(5)/2));
    int t; cin >> t;
    
    while(t--) {
        int y, x; cin >> y >> x;

        solve(y, x);
    }
}